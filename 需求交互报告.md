# Tree-Fusion-Diary · 交互需求报告

> **来源**：逐行阅读三个原始项目源码后整理  
> **格式**：`页面 → 元素 → 操作 → 结果`  
> **版本**：2026-03-10

---

## 一、全局 App Shell

| # | 页面 | 元素 | 操作 | 结果 |
|---|------|------|------|------|
| 1 | 所有页面 | 左侧边栏 · Todo 导航项 | 点击 | 切换到 Todo 看板视图；侧边栏 Todo 项高亮；顶部 accent 色变蓝 `#0079bf` |
| 2 | 所有页面 | 左侧边栏 · 知识树导航项 | 点击 | 切换到知识树视图；高亮变绿 `#00875a` |
| 3 | 所有页面 | 左侧边栏 · 日记本导航项 | 点击 | 切换到日记本视图；高亮变橙红 `#c9481d` |
| 4 | 所有页面 | 左侧边栏 · 设置导航项 | 点击 | 切换到设置页 |
| 5 | 所有页面 | 顶部同步状态点 | — | 显示 Supabase 同步状态（绿色发光 = 已同步，灰色 = 离线中） |
| 6 | 所有页面 | 当前页面任意地方 | 按 `Escape` | 关闭当前打开的 Modal / 弹窗 |

---

## 二、Todo 模块（fusion-todo）

### 2.1 看板头部

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 7 | **Status View** 标签 | 点击 | 看板按「状态」分列（To Do / In Progress / Done / When Free）；卡片左侧4px彩色Border表示状态；Status视图下顶部3px Context色条 |
| 8 | **Context View** 标签 | 点击 | 看板按「清单」分列（Urgent / Deep Work / Routine）；列顶部有彩色边框；每张卡片底部有 Status 下拉选择器 |
| 9 | **Date View** 标签 | 点击 | 看板按日期分列，任务按日期分配 |
| 10 | 🔃 布局切换按钮 | 点击 | 列布局在横向/纵向叠放之间切换 |
| 11 | **➕ New** 按钮 | 点击 | 弹出「新建任务」Modal，含任务名称、状态、Context 下拉 |

### 2.2 看板列操作

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 12 | 列标题输入框 | 单击聚焦 | 变为白色背景 + 蓝色边框，进入可编辑 |
| 13 | 列标题输入框 | 输入新标题 + 失焦 | 列标题更新，同步保存 |
| 14 | 列头区域（非标题） | 鼠标按住拖拽 | 整列横向拖拽重排，其他列实时让位，持久化顺序 |
| 15 | **折叠** 按钮 | 点击 | 列内任务收起；按钮变为「展开」 |
| 16 | **展开** 按钮 | 点击 | 列内任务重新展开 |
| 17 | **···** 动作按钮 | 点击 | 展开动作面板：← / → / 删除（仅 Context 视图） |
| 18 | 动作面板 · **← 向左** | 点击 | 该列向左移动一位 |
| 19 | 动作面板 · **→ 向右** | 点击 | 该列向右移动一位 |
| 20 | 动作面板 · **删除** | 点击 | 弹出确认「删除该列表及其所有任务？」，确认后删除 |
| 21 | 列右上角 · 数量徽章 | — | 实时显示当前列任务数量 |
| 22 | **+ Add card** 按钮 | 点击 | 弹出「新建任务」Modal，预填当前列 ID |
| 23 | **+ Add List** 按钮（Context 视图底部） | 点击 | 新建 Context 列 |

### 2.3 任务卡片

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 24 | 任务卡片 | 单击 | 弹出「Edit Task」Modal |
| 25 | 任务卡片 | 鼠标按住拖拽 | 原位 ghost 占位；鼠标跟随拖拽图；可跨列拖放；落点后更新 status 或 context |
| 26 | 卡片底部 · Status 下拉（非 Status 视图） | 选择新状态 | 任务状态更新，卡片左侧 border 颜色同步 |

### 2.4 新建任务 Modal

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 27 | 「新建任务」Modal | 打开 | 弹窗出现：任务名称 textarea（自动聚焦）、状态下拉、Context 下拉 |
| 28 | **创建** 按钮 | 点击 | 名称不为空时创建任务，Modal 关闭 |
| 29 | **取消** 按钮 / 遮罩 | 点击 | Modal 关闭不保存 |

### 2.5 编辑任务 Modal（Edit Task）

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 30 | 「Edit Task」Modal | 打开 | 显示：任务描述 textarea、Status 下拉、Context（LIST）下拉、Priority/Color 色块、Tags 输入框 |
| 31 | 色块选择器 | 点击某色块 | 选中块高亮（放大+环形边框），任务卡片背景色预览更新 |
| 32 | Tags 输入框 | 输入逗号分隔的标签 | 解析为标签数组，卡片显示对应颜色徽章 |
| 33 | **Save Changes** 按钮 | 点击 | 保存所有字段，Modal 关闭，看板卡片即时刷新 |
| 34 | **Delete Task** 按钮 | 点击 | 任务从看板删除，Modal 关闭 |
| 35 | **Cancel** / **×** / `Escape` | 点击/按键 | Modal 关闭，不保存 |

### 2.6 记录想法 Modal（💡 IdeaModal）

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 36 | 「记录新想法」Modal | 打开 | 显示：标题 textarea（自动聚焦）、创建日期 date picker、关联清单下拉、标签输入区 |
| 37 | 标签输入框 | 输入 + `Enter` | 标签添加为蓝色徽章 |
| 38 | 标签徽章 · × | 点击 | 删除该标签 |
| 39 | **保存想法** 按钮 | 点击 | 标题不为空时，创建任务（status 默认 `todo`），Modal 关闭 |

---

## 三、知识树模块（tree-index碎片化人类）

### 3.1 侧边栏（Sidebar）

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 40 | 折叠侧边栏 **◀** 按钮 | 点击 | 侧边栏折叠，左上角出现小展开按钮 |
| 41 | 展开侧边栏 **▶** 按钮 | 点击 | 侧边栏展开恢复 |
| 42 | App 名称文字 | Hover 显示 ✏️ | 点击 ✏️ 进入名称编辑；输入框替换文字 |
| 43 | App 名称输入框 | `Enter` / 失焦 | 保存；`Escape` 取消 |
| 44 | 🔍 搜索框 | 输入关键词 | 文档列表实时过滤（匹配标题 + searchableText 全文内容） |
| 45 | 搜索框 · **×** 按钮 | 点击 | 清空搜索，恢复全部列表 |
| 46 | **＋** 新建文档按钮 | 点击 | 创建新文档（根节点+空子节点+随机 emoji），自动切换并聚焦第一个节点 |
| 47 | 文档列表 · 文档项 | 单击 | 自动保存当前文档 → 加载目标文档 → 大纲区刷新；侧边栏对应项高亮 |
| 48 | 文档项 · Hover 显示 ✏️ | 点击 | 文档标题变为内联编辑输入框 |
| 49 | 文档标题输入框 | 输入 + 失焦 / `Enter` | 重命名文档，保存到 IndexedDB/Supabase；`Escape` 取消 |
| 50 | 文档项 · Hover 显示 🗑️ | 点击 | 文档软删除（移入回收站），从活跃列表消失 |
| 51 | 底部 **回收站** 入口 | 点击 | 切换到回收站视图，显示已删除文档及删除时间 |
| 52 | 回收站 · 文档项 · ♻️ | 点击 | 文档恢复到活跃列表 |
| 53 | 回收站 · 文档项 · ✕ | 点击 | Toast 确认「确定永久删除？」→ 确认后彻底删除 |
| 54 | 回收站头部 · **清空** 按钮 | 点击 | Toast 确认「清空回收站（X个）？」→ 确认后全部永久删除 |

### 3.2 顶部工具栏（Header）

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 55 | 文档标题（面包屑） | 单击 | 标题变为可编辑输入框 |
| 56 | 文档标题输入框 | 输入 + `Enter` / 失焦 | 保存标题同步到侧边栏；`Escape` 取消 |
| 57 | 💾 **保存** 按钮 | 点击 | 保存到 IndexedDB（+ Supabase 若配置），按钮显示 ⏳ → ✓ / ✗ |
| 58 | ↶ **撤销** 按钮 | 点击 | 撤销上次操作；无历史时灰显不可点 |
| 59 | ↷ **重做** 按钮 | 点击 | 重做上次撤销 |
| 60 | 📤 **导入** 按钮 | 点击 | 打开文件选择器（`.json`）→ 选择后确认弹框「X个节点，当前内容覆盖，继续？」→ 确认后加载 |
| 61 | ⬇️ **导出** 按钮 | 点击 | 展开下拉菜单：「导出 JSON」和「导出 HTML」 |
| 62 | 导出菜单 · **导出 JSON** | 点击 | 触发下载 `文档名.json`（含节点树+设置） |
| 63 | 导出菜单 · **导出 HTML** | 点击 | 触发下载 `文档名.html`（可独立浏览单页） |
| 64 | ☑️ **多选模式** 按钮 | 点击 | 进入节点多选状态；按钮变蓝高亮；节点 bullet 变为复选框；选中数量显示在按钮旁 |
| 65 | ✨ **AI 智能重组** 按钮 | 点击 | 弹出 AIReorganizeModal，对文档（或选中节点）进行 AI 重组 |
| 66 | 行间距控制器 | 调节 | Compact / Normal / Relaxed / Loose 四档，实时改变节点行间距 |
| 67 | ⚙️ **设置** 按钮 | 点击 | 弹出 SettingsModal（深色模式、自动保存等开关） |

### 3.3 大纲节点（OutlineNode）

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 68 | 节点内容区（渲染态） | 单击 | 节点变为 `<input>` 编辑模式，自动聚焦 |
| 69 | 节点输入框 | 输入文字 | 实时更新 Zustand store |
| 70 | 节点输入框 | `Enter`（非 Shift/Ctrl） | 在下方创建同级节点并聚焦 |
| 71 | 节点输入框 | `Ctrl + Enter` | 在下方创建子节点并聚焦 |
| 72 | 节点输入框 | `Tab` | 节点缩进（成为上一节点的子节点） |
| 73 | 节点输入框 | `Shift + Tab` | 节点反缩进（升一层） |
| 74 | 节点输入框 | `Backspace`（内容为空） | 删除当前节点，焦点移到上一节点 |
| 75 | 节点输入框 | 鼠标拖拽选中文字 | 浮现格式工具栏（B / I / U / H 高亮） |
| 76 | 格式工具栏 · **B** | 点击 | 选中文字加粗 |
| 77 | 格式工具栏 · **I** | 点击 | 选中文字斜体 |
| 78 | 格式工具栏 · **U** | 点击 | 选中文字下划线 |
| 79 | 格式工具栏 · **H** | 点击 | 选中文字黄色高亮 |
| 80 | 节点 · 鼠标悬停 > 1秒 | Hover 停留 | 浮现操作工具栏（Operation Toolbar）at 鼠标位置 |
| 81 | 操作工具栏 · **⤵️ 添加子节点** | 点击 | 创建子节点并聚焦 |
| 82 | 操作工具栏 · **➕ 添加同级** | 点击 | 创建同级节点并聚焦 |
| 83 | 操作工具栏 · **→ 缩进** | 点击 | 等同 `Tab` |
| 84 | 操作工具栏 · **← 反缩进** | 点击 | 等同 `Shift + Tab` |
| 85 | 操作工具栏 · **😊 添加图标**（无图标时） | 点击 | 给节点添加默认 📄 图标 |
| 86 | 操作工具栏 · **📷 图片上传** | 点击 | 压缩→上传图床→嵌入节点下方展示 |
| 87 | 节点 emoji 图标 | 单击 | 弹出 IconPicker 图标选择器 |
| 88 | IconPicker | 选择 emoji | 节点图标更新，关闭 Picker |
| 89 | 节点 bullet（has children） | 单击 | 折叠/展开子树（toggle collapse） |
| 90 | 节点 bullet（多选模式中） | 单击 | 切换该节点多选选中状态 |
| 91 | 节点 | 鼠标长按拖拽（@dnd-kit） | 节点可拖拽重排（verticalListSortingStrategy），支持跨层级拖入 |

---

## 四、日记本模块（diary-app）

### 4.1 日记列表页

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 92 | **+ 新建** 按钮 | 点击 | 切换到编辑器页，清空标题和内容，聚焦内容区 |
| 93 | **📝 周记** 按钮 | 点击 | 跳转到「选择日记→生成周记」流程（见4.5节） |
| 94 | 日记卡片 | 单击 | 打开沉浸阅读 Viewer |
| 95 | 日记卡片 · **查看** 按钮 | 点击 | 打开 Viewer（同上） |
| 96 | 日记卡片 · **编辑** 按钮 | 点击 | 切换到编辑器，加载该日记（标题、正文、图片） |
| 97 | 日记卡片 · **删除** 按钮 | 点击 | `confirm` 对话框「确定删除？」→ 确认后删除，列表刷新 |
| 98 | 侧边栏「选择」Tab | 点击 | 进入多选模式，每条日记左侧出现复选框 |
| 99 | 选择模式 · **全选** 按钮 | 点击 | 全部日记勾选 |
| 100 | 选择模式 · **清空** 按钮 | 点击 | 取消所有勾选 |
| 101 | 选择模式 · **生成周记** 按钮 | 点击 | 将勾选日记传入「生成周记」Modal |

### 4.2 日记编辑器页

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 102 | 标题输入框 | 输入 | 更新标题；若为空自动从正文提取前30字 |
| 103 | 正文 textarea | 输入 | 实时更新底部字数统计 |
| 104 | 正文 textarea | 粘贴图片 URL（`https://...jpg/png/gif/webp`） | 自动识别为图片，插入正文图片区，Toast「已自动识别并插入图片链接」 |
| 105 | 格式工具栏 · **B / I / U / H1 / H2 / 列表** | 点击 | 对选中文字应用对应格式 |
| 106 | **📷 插入图片** 按钮 | 点击 | 弹出插入图片 Modal：输入 URL，点确认后图片加入头图区 |
| 107 | **⬆️ 上传图片** 按钮 | 点击 | 打开文件选择器→自动压缩（若超过 maxSizeMB）→上传到图床→显示进度 `上传中 XX%` |
| 108 | 图片上传 · **重试** 按钮（失败时出现） | 点击 | 用上次文件重试上传 |
| 109 | 图片预览区 · **×** | 点击 | 从图片列表移除该图片 |
| 110 | **📄 原文** 按钮 | 点击 | 弹出 Modal 显示日记原始版本（结构优化前） |
| 111 | **仅保存** 按钮 | 点击 | 保存内容但不触发 AI 分析，返回列表页 |
| 112 | **结构优化** 按钮 | 点击 | 启动 AI 结构优化，显示进度条，完成后弹出结构版本 Modal |
| 113 | **✨ 保存并分析** 按钮 | 点击 | 保存 → 进度条 → AI 情感分析+改写 → 弹出 Analysis Modal |
| 114 | 进度条 · **取消** 按钮 | 点击 | 中止 AI 请求，隐藏进度条，Toast「已取消分析」 |
| 115 | **后脚注图片上传** | 点击 | 上传图片到日记尾图区域（独立于头图） |

### 4.3 AI 分析结果 Modal

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 116 | Analysis Modal | 打开 | 原文显示（负面句橙色高亮，hover 显示改进建议）+ 右侧乐观改写版本 |
| 117 | **保留原文** 按钮 | 点击 | 以原文为最终版本保存；Modal 关闭；询问「是否继续结构分析？」 |
| 118 | **采用改写版本** 按钮 | 点击 | AI 改写版本替换正文；更新标题；Modal 关闭；询问「是否继续结构分析？」 |
| 119 | **重新生成** 按钮 | 点击 | 按钮变「生成中...」，重新调用 AI，完成后刷新 Modal |
| 120 | **_ 最小化** 按钮 | 点击 | Modal 隐藏（不销毁），可继续编辑正文 |
| 121 | **✕ 关闭** 按钮 | 点击 | Modal 关闭，清理事件绑定 |

### 4.4 结构优化 Modal

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 122 | Structure Modal | 打开 | 显示 AI 生成的结构化版本（段落 + 目录树预览） |
| 123 | **采用此版本** 按钮 | 点击 | 正文替换为结构版本，保存 `structured_version`，Toast「结构优化已保存」 |
| 124 | **取消 / 关闭** 按钮 | 点击 | Modal 关闭，不覆盖正文 |

### 4.5 周记生成（Weekly Summary）

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 125 | 「生成周记」Modal | 打开（Tab① 选择日记） | 列出全部日记，每条有复选框、标题、日期 |
| 126 | 复选框 | 勾选/取消 | 已选计数实时更新「已选 X 篇」 |
| 127 | **全选** 按钮 | 点击 | 全部日记勾选 |
| 128 | **清空** 按钮 | 点击 | 取消所有勾选 |
| 129 | **生成周记 →** 按钮 | 点击（未选则 Toast「请先选至少一篇」） | 切换 Tab②，启动 AI 多阶段进度（读取→分析→撰写→润色→完成） |
| 130 | AI 进度条 | 自动推进 | 各阶段文字轮播，进度条动态填充 |
| 131 | 生成完成后 | — | 显示周记正文；出现操作按钮区 |
| 132 | **📋 复制** 按钮 | 点击 | 内容复制到剪贴板，Toast「已复制」 |
| 133 | **💾 保存为日记** 按钮 | 点击 | 创建新日记条目，Toast「已保存」 |
| 134 | **🔄 重新生成** 按钮 | 点击 | 清空结果，重新调用 AI |
| 135 | 周记 Modal · **插入图片** 按钮 | 点击 | 弹出插入图片 Modal，为周记添加头图 |
| 136 | 周记 Modal · **上传图片** 按钮 | 点击 | 上传图片到周记头图区 |
| 137 | 周记正文区 | 粘贴图片 URL | 自动识别插入头图 |
| 138 | 周记 · **结构优化** 按钮 | 点击 | 对周记内容进行结构优化（同4.4节流程） |

### 4.6 沉浸阅读 Viewer

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 139 | Viewer Modal | 打开 | 全屏沉浸阅读：标题、日期、正文、图片 |
| 140 | **← 上一篇** 按钮 | 点击 | 切换到时间线上一篇日记 |
| 141 | **→ 下一篇** 按钮 | 点击 | 切换到时间线下一篇日记 |
| 142 | **A-** 缩小字号 | 点击 | 正文 -2px（最小 12px） |
| 143 | **A+** 放大字号 | 点击 | 正文 +2px（最大 32px） |
| 144 | **✏️ 编辑** 按钮 | 点击 | Viewer 关闭，进入该日记编辑器 |
| 145 | **✕ 关闭** 按钮 | 点击 | Viewer 关闭，返回列表 |

---

## 五、设置页

| # | 元素 | 操作 | 结果 |
|---|------|------|------|
| 146 | Supabase URL / Anon Key 输入框 + **保存配置** | 点击 | 持久化到 localStorage，同步功能启用 |
| 147 | AI API Key 输入框 + **保存** | 点击 | 保存 API Key，Toast「已保存」，返回列表 |
| 148 | **测试连接** 按钮 | 点击 | 按钮变「测试中...」→ 调用 AI API 验证 → 显示「连接成功」或错误信息 |
| 149 | 图床 Upload URL / Token + **保存图床配置** | 点击 | 保存图床配置，Toast「保存成功」 |
| 150 | **测试图床连接** 按钮 | 点击 | 验证图床，显示连接结果 |
| 151 | 知识树 · **深色模式** 开关 | 切换 | 知识树界面亮色/暗色切换 |
| 152 | 知识树 · **自动保存** 开关 | 切换 | 开启后每次编辑自动保存 |
| 153 | **📂 导入旧版 MyDiary 文件夹** 区域 | 点击 | 触发 File System API 选择文件夹（.json + 图片），批量导入到 Supabase |
| 154 | **🗃️ 导入 todo.db** 区域 | 点击 | 选择 SQLite 文件，自动迁移到 Supabase Tasks 表 |

---

## 六、全局通用

| # | 场景 | 触发条件 | 结果 |
|---|------|----------|------|
| 155 | Toast 通知 | 任何操作完成/失败 | 底部弹出消息条，3秒自动消失 |
| 156 | 离线队列 | 网络断开时点击保存/分析 | 任务加入 OfflineQueue，Toast「已加入离线队列，有网时自动执行」 |
| 157 | 在线恢复 | 网络重新连接 | OfflineQueue 自动继续执行，状态栏「离线」→「在线」 |
| 158 | 模态遮罩 | 点击 Modal 外部区域 | Modal 关闭 |
| 159 | 知识树 | `Ctrl + S` | 保存当前文档 |
| 160 | Todo 拖拽卡片到对应列 | 跨列落放 | task.status / task.context 自动更新为目标列 ID |

---

## 七、数据模型速查

```
Task        { id, title, status, context, tags[], color?, createdAt? }
Status      { id, title, color, collapsed, belowOf? }
Context     { id, title, color, collapsed, belowOf? }
Diary       { id, title, date, content, original_content?,
              structured_version?, finalVersion?,
              images[], footer_images[], analysis? }
Weekly      { id, title, startDate, endDate, summary,
              images[], footer_images?, diaryIds[] }
Document    { id, title, root: OutlineNode,
              metadata: { createdAt, updatedAt, version, deletedAt? } }
OutlineNode { id, parentId, content, level, children[],
              images[], collapsed, isHeader?, isSubHeader?,
              isItalic?, icon?, tags? }
```
