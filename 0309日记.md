# 🌳 Tree-Fusion-Diary 开发日记

## 日期：2026-03-09

### 今日进展总结 (What we did today)

今天的主要工作是为**“四位一体”数字生命系统** (`tree-fusion-dairy`) 进行深度的前端统一原型设计（Prototype）以及底层数据库逻辑的对齐与规划。

#### 1. 统一前端交互原型 (Unified UI Prototyping)
- **搭建高保真单页原型 (`prototype.html`)**：
  - 成功在一个纯 HTML/CSS/JS 文件中，将 `fusion-todo` (任务)、`tree-index` (知识)、`diary-app` (日记) 三个完全不同技术栈的项目在视觉和交互层面上融为一体。
  - **全局 App Shell 层**：确立了左侧统一导航栏、顶部状态区以及居中的主工作区。
- **引入第四大模块：情绪统计 (Mood Stats)**：
  - 从 `Life-Coach-AI` 提取并升华了情绪管理模块，**剥离了原本附属的日记记录逻辑**，使其成为一个极其轻量、记录时间<10秒的纯情绪热力图看板。
  - **视觉对齐**：采用方格背景填充、红绿高对比度情绪色阶设计。
  - **功能升级**：新增了可切换的月份选择器、全月日历网格补齐功能，以及基于 7天/30天/60天/180天 滚动均值的情绪仪表盘。

#### 2. 深度需求挖掘与交互对齐 (Requirement Finalization)
- **文档沉淀 - 需求交互报告 (`需求交互报告.md`)**：
  - 通过对三个原始项目的源代码进行逐行级穿透，整理出了一份极度详细的交互规则集。
  - 将每个页面的 UI 元素、操作行为、预期结果和键盘快捷键做成了完整的 Mapping List。
  - 明确了 AI 重组、AI 周记分析等前端微交互（Toast、持久化缓存状态、进度条等）的边界。

#### 3. 离线优先数据库架构设计 (Offline-First Schema Layout)
- **文档沉淀 - Supabase 数据库设计参考 (`Supabase_Schema_Reference.md`)**：
  - 基于 Next.js + Supabase + Dexie.js (Zustand) 的 Offline-First 架构，敲定了核心存储表结构。
  - **Todo 强联动设计**：重新审视 `fusion-todo` 结构，将任务状态（Status）和情境（Context）从简单的字段独立为三表 (`todo_statuses`, `todo_contexts`, `todo_tasks`)，并通过 `ON DELETE RESTRICT` 防止“孤儿卡片”问题，同时加入了 `order_index` 保证前端跨列拖拽排序的最终一致性。
  - **Tree Index 序列化策略**：为了避免 IndexedDB 频繁同步时的递归嵌套查询瓶颈，创造性地将整棵知识树设计存入单一的 **JSONB** 字段，实现极其高效的单行无锁更新。
  - **Diary 结构细化**：细化了纯净原文 (`original_content`)、草稿 (`content`)、结构化版本 (`structured_version`)、定稿 (`final_version`) 等多版本内容，并拆分了头图和尾图的 Supabase URL 关联设计。
  - **全局图床与 AI 配置 (`user_settings`)**：在剥离原生项目的 `localStorage` 强依赖后，专门设计了独立的用户配置表，允许用户存入**自定义图床 (Upload URL/Token)** 和自带的 AI API Keys。

### 下一步计划 (Next Steps)
1. **真实框架落地**：将单页 `prototype.html` 的 UI 代码抽取为 Next.js + TailwindCSS 组件，落实到新项目的 `app/(private)/` 下属目录。
2. **状态管理初始化**：引入 Zustand store 和 Dexie DB，将原型中的 Mock 数据转化为可持久化的离线查询。
3. **后端搭建验证**：使用写好的 Schema Reference 在 Supabase Dashboard 执行连表 SQL 建库，同时编写 RLS 策略。
4. **功能迁移对接**：将原先项目内的复杂逻辑（如拖拽、AI 提示词通信等）逐一复刻并连通新 API。
