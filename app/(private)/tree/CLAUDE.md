# app/(private)/tree/ · L2 局部地图

> **[PROTOCOL]**: 本文档为 GEB 分形文档 L2 层，变更时更新此文件，然后检查根 CLAUDE.md。

## 职责

知识树（Knowledge Tree）展示与编辑域。提供 Outliner 风格的笔记管理体验，支持无限层级缩进与实时同步。

## 成员清单

| 文件 | 职责 | 备注 |
|---|---|---|
| `page.tsx` | 知识树主页面入口 | |

## 暴露接口

此目录主要作为路由入口，不直接导出业务组件。业务逻辑由 `components/tree/` 承载。

## 约束

- 交互逻辑应尽可能保留在 `components/` 目录中。
- 遵循 L1 质量红线。
