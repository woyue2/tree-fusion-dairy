# Anti-Huihuan 前后端目录规范

> **用途**：每次 `/implement` 或 `/review` 时，对照本文档检查目录结构是否合规。
> **最后更新**：2026-03-08（架构重建：移除 FastAPI，采用 Next.js Server Actions + Supabase）

---

## 一、架构总览

本项目为 **纯 Next.js 单体架构**，无独立后端服务。

| 机制 | 用途 |
|---|---|
| **Server Component (`page.tsx`)** | SSR 数据读取，服务端直连 Supabase |
| **Server Actions (`app/actions/`)** | 数据变更（写入），替代传统 REST API |
| **Route Handlers (`app/api/`)** | 仅用于 Webhook / 文件上传 / 第三方回调 |
| **Supabase** | PostgreSQL 数据库 + Auth + Storage |

> ⚠️ **FastAPI 后端已移除**，不再维护 `backend/` 目录规范。

---

## 二、目录规范（Next.js App Router）

### 2.1 目标结构

```
anti-huihuan-kaifa/
│
├── app/
│   ├── layout.tsx                  ← Server Component（禁止 'use client'）
│   ├── page.tsx                    ← async Server Component（SSR 取数）
│   ├── globals.css
│   ├── error.tsx                   ← Client Component（'use client' 必须）
│   ├── loading.tsx
│   │
│   ├── actions/                    ← Server Actions
│   │   ├── index.ts                ← re-export 聚合
│   │   └── {domain}.ts             ← 每个业务域一个文件（'use server' 在文件顶部）
│   │
│   ├── api/                        ← Route Handlers（非 Server Actions 场景）
│   │   └── {feature}/route.ts
│   │
│   └── (route-groups)/             ← 路由分组（受保护页面）
│       └── {feature}/
│           ├── page.tsx            ← async Server Component
│           ├── loading.tsx
│           ├── error.tsx
│           └── components/         ← 路由私有组件（不跨路由复用）
│
├── components/                     ← 公共 UI 组件（跨路由复用）
│   ├── ui/                         ← 原子组件（Button, Input, Modal...）
│   └── {domain}/                   ← 业务域组件
│
├── hooks/                          ← 自定义 React Hooks
│   └── use{Domain}Store.ts         ← Zustand 状态（按业务域拆分）
│
├── lib/                            ← 外部服务 + 工具
│   ├── supabase.ts                 ← 客户端 Supabase（浏览器，持久 session）
│   ├── supabase-server.ts          ← 服务端 Supabase（Server Actions 专用）
│   ├── supabase-db.ts              ← 数据操作封装（所有表 CRUD 集中）
│   ├── constants.ts                ← 全局常量
│   ├── api-utils.ts                ← Route Handler 响应工具
│   └── validation.ts               ← Zod schemas
│
├── types/                          ← 全局 TypeScript 类型（独立目录）
│   └── index.ts
│
├── utils/                          ← 纯工具函数（无副作用）
│   └── index.ts
│
└── middleware.ts                   ← Supabase Auth 路由守卫
```

### 2.2 铁律

| 编号 | 规则 | 说明 |
|------|------|------|
| F-01 | **组件 ≤ 150 行** | 超出必须拆分子组件 |
| F-02 | **`'use client'` 最小化** | 默认 Server Component，只有使用 `useState`/`useEffect`/事件监听时加 |
| F-03 | **`layout.tsx` 禁止 `'use client'`** | 根 layout 必须是 Server Component |
| F-04 | **每个路由必须有 `loading.tsx` + `error.tsx`** | 防止 Suspense 缺失和页面白屏 |
| F-05 | **每个 `page.tsx` 必须导出 `metadata`** | SEO 基本要求 |
| F-06 | **Props 必须有接口定义** | 禁止 `props: any`，超过 3 个字段不允许内联匿名类型 |
| F-07 | **禁止 `any`（含 `as any`）** | 用 `unknown` + 类型收窄替代 |
| F-08 | **`useState` 泛型显式标注** | `useState<T \| null>(null)` ✅ · `useState(null)` ❌ |
| F-09 | **内部跳转用 `next/link` 或 `useRouter`** | 禁止 `window.location.href` |
| F-10 | **禁止生产代码 `console.log`** | 错误用 `console.error` |
| F-11 | **共享类型/常量/工具提升到 `lib/` 或 `types/`** | 禁止跨路由直接 import |
| F-12 | **Hook 以 `use` 开头，事件处理以 `handle` 开头** | 命名规范 |

---

## 三、Server Actions 规范

### 3.1 结构规范

```typescript
// app/actions/{domain}.ts

'use server'  // ← 必须在文件顶部，不允许逐函数标注

// INPUT: 参数类型
// OUTPUT: 返回类型
import { revalidatePath } from 'next/cache'
import { createSupabaseServerClient } from '@/lib/supabase-server'

export async function createItem(input: CreateItemInput) {
  const supabase = createSupabaseServerClient()  // ← 服务端 client
  const { error } = await supabase.from('items').insert(input)
  if (error) throw error
  revalidatePath('/')  // ← 刷新 SSR 数据
}
```

### 3.2 铁律

| 编号 | 规则 |
|------|------|
| A-01 | `'use server'` 必须在文件顶部（文件级声明） |
| A-02 | Server Actions 中必须使用 `lib/supabase-server.ts`，禁止用 `lib/supabase.ts` |
| A-03 | 数据操作通过 `lib/supabase-db.ts` 封装，不在 actions 里直接写 SQL |
| A-04 | 变更后必须调用 `revalidatePath()` 刷新缓存 |
| A-05 | 参数必须用 Zod 验证（`lib/validation.ts` 中的 schema） |

---

## 四、Supabase 双 Client 规范

| Client | 文件 | 环境 | 用途 |
|---|---|---|---|
| 客户端 | `lib/supabase.ts` | 浏览器 | Auth 登录登出、Realtime 订阅 |
| **服务端** | `lib/supabase-server.ts` | 服务端 | Server Actions 写入、Server Components SSR 取数 |

**铁律**：

| 编号 | 规则 |
|------|------|
| S-01 | Server Actions 中禁止 import `lib/supabase.ts`（无 cookies，无法鉴权） |
| S-02 | 客户端组件中禁止 import `lib/supabase-server.ts`（服务端模块，会报错） |
| S-03 | `supabase-server.ts` 使用 `@supabase/ssr` 的 `createServerClient` |
| S-04 | Supabase RLS（Row Level Security）必须启用，不依赖应用层鉴权 |

---

## 五、'use client' 边界决策表

| 文件类型 | 默认 | 加 `'use client'` 条件 |
|----------|------|------------------------|
| `layout.tsx` | Server | **永远不加** |
| `page.tsx` | Server | 有 `useState` / `useEffect` / 事件监听时加，但优先拆出子组件 |
| `components/*.tsx` | Server | 有交互逻辑时加，尽量推迟到叶子节点 |
| `hooks/use*.ts` | — | **必须加** |
| `app/actions/*.ts` | — | **使用 `'use server'` 而非 `'use client'`** |

---

## 六、检查清单

### 6.1 目录结构检查

```
□ 是否存在非规范目录（如 backend/、frontend/ 等旧结构残留）？
□ app/actions/ 中的文件是否都有 'use server' 顶部声明？
□ lib/ 目录文件数是否超过 8 个？（超过需拆分子目录）
□ 是否有跨路由直接 import（如 ../../other-route/store）？
□ components/ 是否按业务域分组（components/{domain}/）？
```

### 6.2 代码质量检查

```bash
# 检查超过 150 行的 .tsx 文件
Get-ChildItem -Recurse -Filter "*.tsx" | Where-Object { (Get-Content $_.FullName).Count -gt 150 } | Select-Object FullName

# 检查 any 使用
Select-String -Recurse -Include "*.tsx","*.ts" -Pattern ": any|as any" .

# 检查 console.log
Select-String -Recurse -Include "*.tsx","*.ts" -Pattern "console\.log" .

# 检查 window.location
Select-String -Recurse -Include "*.tsx","*.ts" -Pattern "window\.location" .
```

### 6.3 GEB 文档检查

```
□ 新增文件是否有 L3 头部注释（INPUT/OUTPUT/POS/DEPS）？
□ 新增模块目录是否有 CLAUDE.md（L2）？
□ 删除文件是否已从 L2 成员清单移除？
□ 重构目录是否已更新 CLAUDE.md（L1）目录结构图？
```

---

## 七、变更触发规则

| 触发事件 | 需要检查的项 |
|----------|-------------|
| 新建 `.tsx` 文件 | → 是否 ≤ 150 行？是否有 Props 接口？是否需要 `'use client'`？ |
| 新建 `actions/*.ts` | → 是否有 `'use server'`？是否用了服务端 Supabase client？ |
| 新建路由目录 | → 是否配套 `loading.tsx` + `error.tsx` + `metadata`？ |
| 新建模块目录 | → 是否创建了 GEB L2 `CLAUDE.md`？ |
| 删除文件 | → 是否更新了 L2 成员清单？ |
| 修改类型 | → `types/index.ts` 是否同步？ |
| 跨路由 import | → 是否应该提升到 `lib/` 或 `types/`？ |
