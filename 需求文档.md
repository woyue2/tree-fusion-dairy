# Tree-Fusion-Dairy · 项目需求文档

> **[PROTOCOL]**: 本文档为项目核心功能需求定义，任何涉及核心逻辑变更的开发必须同步更新此文档。

---

## 一、 项目背景与目标

**Tree-Fusion-Dairy** 旨在构建一个“四位一体”的数字生命管理系统。它将**碎片化任务 (Todo)**、**层级化知识 (Tree)**、**结构化记录 (Diary)** 与 **情绪健康 (Stats)** 深度缝合，提供极致丝滑的离线的优先（Offline-First）体验。

### 核心目标
1.  **体验统一**：打破工具孤岛，让任务、笔记、日记在同一个 App Shell 中流转。
2.  **离线优先 & 云端同步 (Requirement A)**：基于 IndexedDB 保证瞬时响应，并通过 Supabase 实现跨端感知。
3.  **情绪感知 (Requirement B)**：通过轻量化记录，量化心理健康状况。

---

## 二、 核心功能模块

### 1. Fusion Todo (任务看板)
借鉴 Trello 交互，提供多维度的任务组织形式。
*   **多视图切换**：提供 Status (状态流)、Context (场景清单)、Date (时间轴) 三种视图。
*   **极致交互**：支持卡片跨列拖拽、列排序、内联快速创建、详细 Modal 编辑。
*   **任务属性**：支持 Priority、Tags、Color 背景、Status/Context 联动。

### 2. Knowledge Tree (知识树)
Outliner 风格的碎片化知识整理工具。
*   **无限层级大纲**：支持经典的 Outliner 快捷键（Tab 缩进、Shift+Tab 反缩进、Enter 创建同级、Ctrl+Enter 创建子级）。
*   **高效存储**：采用 JSONB 单行存储策略，解决大规模节点树的同步性能瓶颈。
*   **文档管理**：支持多文档并行、回收站机制、JSON/HTML 导出。

### 3. Structured Diary (结构化日记)
从原始记录到生成定稿的知识演化过程。
*   **多版本管理**：保存 原文 -> 草稿 -> 结构化 -> 定稿 的演化路径。
*   **图文并茂**：支持内联粘贴 URL 图片解析，集成全局图床上传能力。
*   **沉浸阅读**：提供全屏 Viewer，支持字号调节和时间线快速切换。

### 4. Mood Stats (情绪统计 - Requirement B)
**设计哲学**：记录时间 < 10 秒，极简却极具洞察力。
*   **热力图看板**：仿 GitHub 风格，用红绿高对比度色阶展示整月情绪分布。
*   **仪表盘分析**：计算 7天 / 30天 / 60天 / 180天 的滚动情绪均值（Rolling Average），识别心理周期。
*   **连续记录**：显示连续打卡天数，建立正面行为反馈。

---

## 三、 核心技术要求 (Tech Requirements)

### 1. 数据流与同步 (Requirement A)
*   **单点真实源**：Supabase 替代所有 Mock 数据。
*   **离线架构**：Store 层通过 Zustand 维护 UI 状态，并与 Dexie.js (IndexedDB) 进行本地持久化。
*   **重连同步**：网络恢复后，自动将离线队列（OfflineQueue）中的任务推送到 Supabase。

### 2. UI/UX 设计
*   **高级美学**：遵循 TailwindCSS v4 色值规范，主色调按模块区分（Todo-蓝, Tree-绿, Diary-红, Stats-灰）。
*   **微动画**：侧边栏折叠、Modal 弹出、列表切换需带有平滑过渡。
*   **响应式**：适配桌面端生产力场景。

---

## 四、 核心排除逻辑 (Non-Requirements / Scheme C)

以下功能**不属于**本阶段开发范畴：
1.  **AI 跨模块全自动重组**：不支持从日记中通过 AI 自动解析并创建 Todo 任务或 Tree 知识节点。
2.  **后端复杂计算**：所有统计计算（如滚动均值）在前端 Client 侧完成，Supabase 仅做数据落盘。

---

## 五、 GEB 协议红线

1.  **文档同构**：任何代码变更必须同步更新 L1 (CLAUDE.md) 和 L2 (Module CLAUDE.md)。
2.  **接口契约**：L3 头部注释必须包含 INPUT/OUTPUT/POS/DEPS。
3.  **代码指标**：单文件 < 800 行，单函数 < 20 行，缩进 < 3 层。

---

_Last updated: 2026-03-15 by Antigravity_
